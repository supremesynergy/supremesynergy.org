# /downloads

Files served publicly at `https://supremesynergy.org/downloads/<filename>`.

The thank-you page (`/thanks`) currently links to `sim-one-page.pdf`. Put the
file here with exactly that name, or rename it and update the `href` in
`thanks.html` (it is marked with a comment block).

Anything in this folder is reachable by direct URL. Do not put anything here
that is meant to stay private.
